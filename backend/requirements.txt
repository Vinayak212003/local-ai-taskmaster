# LocalAI TaskMaster — Backend Dependencies
# Tested on Python 3.10+ | CPU-optimized, no GPU required

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Async SQLite ORM
sqlalchemy==2.0.31
aiosqlite==0.20.0

# Config + validation
pydantic==2.7.4
pydantic-settings==2.3.4

# HTTP client (Ollama + async)
httpx==0.27.0

# YAML for prompt templates
pyyaml==6.0.1

# Dev / testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx  # already above, used in tests too
