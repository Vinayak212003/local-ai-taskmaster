# LocalAI TaskMaster — Environment Configuration
# Copy this to .env and adjust as needed.
# All values below are defaults for local development.

# ── Server ────────────────────────────────────────────────
APP_NAME="LocalAI TaskMaster"
APP_VERSION="1.0.0"
DEBUG=true
HOST=0.0.0.0
PORT=8000

# ── Ollama ────────────────────────────────────────────────
# Make sure Ollama is running: ollama serve
OLLAMA_BASE_URL=http://127.0.0.1:11434

# Model to use (must be pulled first via: ollama pull <model>)
# Recommended for 8GB RAM: mistral, phi3, gemma2:2b
# More capable (needs 16GB): llama3, mixtral
OLLAMA_MODEL=mistral

# Request timeout in seconds (increase for slow machines)
OLLAMA_TIMEOUT=180

# Context window — keep at 4096 for 8GB RAM
OLLAMA_NUM_CTX=4096

# ── Agent Limits ──────────────────────────────────────────
MAX_SUBTASKS=6
PLANNER_MAX_TOKENS=600
EXECUTOR_MAX_TOKENS=1200
VALIDATOR_MAX_TOKENS=500
